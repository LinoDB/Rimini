{
  "configurations": [
    {
      "name": "compile and run",
      "type": "lldb",
      "request": "launch",
      "program": "${cwd}/out/Rimini",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "compile build"
    },
    {
      "name": "compile and test",
      "type": "lldb",
      "request": "launch",
      "program": "${cwd}/out/tests",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "compile tests",
    },
  ],
  "version": "2.0.0"
}