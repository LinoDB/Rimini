{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build file",
            "command": "g++",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "src/**",
                "-o",
                "${cwd}/out/Rimini",
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}